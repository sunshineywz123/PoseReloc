# @package _global_
type: hloc

match_type: softmax
suffix: ''

network:
    detection: superpoint
    matching: superglue

model:
    pretrain_model_path: ${data_dir}/models/checkpoints/${task_name}/epoch=19.ckpt
    extractor_model_path: ${data_dir}/models/extractors/SuperPoint/superpoint_v1.pth
    match_model_path: ${data_dir}/models/matchers/SuperGlue/superglue_outdoor.pth

input:
    data_dir: ${data_dir}/scan_data/007-bluemoon-bottle/lyl4
    sfm_model_dir: ${data_dir}/sfm_model/007-bluemoon-bottle

output:
    vis_dir: '/home/wangzihao/runs/vis3d/PoseReloc'